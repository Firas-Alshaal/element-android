<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <im.vector.app.features.location.MapTilerMapView
        android:id="@+id/mapView"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:maplibre_renderTextureMode="true"
        app:showLocateButton="true"
        tools:background="#4F00" />

    <!-- Map Control Panel - Top Right -->
    <LinearLayout
        android:id="@+id/mapControlPanel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:layout_marginEnd="20dp"
        android:background="@drawable/bg_map_control_panel"
        android:orientation="vertical"
        android:padding="12dp"
        android:elevation="12dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <!-- Map Type Selector -->
        <LinearLayout
            android:id="@+id/mapTypeSelector"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:background="@drawable/bg_map_type_selector"
            android:padding="8dp"
            android:layout_marginBottom="12dp">

            <ImageButton
                android:id="@+id/btnMapStandard"
                android:layout_width="36dp"
                android:layout_height="36dp"
                android:background="@drawable/bg_map_type_button"
                android:src="@drawable/ic_map_standard"
                android:contentDescription="خريطة قياسية"
                android:scaleType="centerInside"
                android:padding="8dp"
                android:layout_marginEnd="6dp" />

            <ImageButton
                android:id="@+id/btnMapSatellite"
                android:layout_width="36dp"
                android:layout_height="36dp"
                android:background="@drawable/bg_map_type_button"
                android:src="@drawable/ic_map_satellite"
                android:contentDescription="خريطة فضائية"
                android:scaleType="centerInside"
                android:padding="8dp"
                android:layout_marginEnd="6dp" />

            <ImageButton
                android:id="@+id/btnMapNight"
                android:layout_width="36dp"
                android:layout_height="36dp"
                android:background="@drawable/bg_map_type_button"
                android:src="@drawable/ic_map_night"
                android:contentDescription="خريطة ليلية"
                android:scaleType="centerInside"
                android:padding="8dp" />

        </LinearLayout>

        <!-- Zoom Controls -->
        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:background="@drawable/bg_zoom_controls"
            android:padding="8dp">

            <ImageButton
                android:id="@+id/btnZoomIn"
                android:layout_width="36dp"
                android:layout_height="36dp"
                android:background="@drawable/bg_zoom_button"
                android:src="@drawable/ic_zoom_in"
                android:contentDescription="تكبير"
                android:scaleType="centerInside"
                android:padding="8dp"
                android:layout_marginBottom="6dp" />

            <ImageButton
                android:id="@+id/btnZoomOut"
                android:layout_width="36dp"
                android:layout_height="36dp"
                android:background="@drawable/bg_zoom_button"
                android:src="@drawable/ic_zoom_out"
                android:contentDescription="تصغير"
                android:scaleType="centerInside"
                android:padding="8dp" />

        </LinearLayout>

    </LinearLayout>

    <ImageView
        android:id="@+id/shareLocationPin"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:contentDescription="@string/a11y_location_share_pin_on_map"
        app:layout_constraintBottom_toTopOf="@id/shareLocationMapCenter"
        app:layout_constraintEnd_toEndOf="@id/mapView"
        app:layout_constraintStart_toStartOf="@id/mapView" />

    <ViewStub
        android:id="@+id/shareLocationMapCenter"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintBottom_toBottomOf="@id/mapView"
        app:layout_constraintEnd_toEndOf="@id/mapView"
        app:layout_constraintStart_toStartOf="@id/mapView"
        app:layout_constraintTop_toTopOf="@id/mapView" />

    <im.vector.app.features.location.option.LocationSharingOptionPickerView
        android:id="@+id/shareLocationOptionsPicker"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

    <ProgressBar
        android:id="@+id/shareLocationGpsLoading"
        android:layout_width="32dp"
        android:layout_height="32dp"
        android:layout_marginEnd="@dimen/layout_horizontal_margin"
        android:layout_marginBottom="@dimen/layout_vertical_margin"
        app:layout_constraintBottom_toBottomOf="@id/shareLocationOptionsPicker"
        app:layout_constraintEnd_toEndOf="@id/shareLocationOptionsPicker" />

    <im.vector.app.features.location.MapLoadingErrorView
        android:id="@+id/shareLocationMapLoadingError"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:visibility="gone"
        app:layout_constraintBottom_toTopOf="@id/shareLocationOptionsPicker"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
